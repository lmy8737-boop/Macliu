# Changelog

## v2.3.1 — 2026-09-08

**新增**：正式给出两个版本供选择，打了 git tag 并新建了 `methodology-only` 分支：

- **`main` 分支 / [v2.3.0](https://github.com/lmy8737-boop/Macliu/releases/tag/v2.3.0)（傻瓜化全自动版）**：本仓库即 Obsidian 知识库，`INSTALL.md` 含 Obsidian 自动部署，适合从零开始的客户。
- **`methodology-only` 分支 / [v2.2.0](https://github.com/lmy8737-boop/Macliu/releases/tag/v2.2.0)（方法论版）**：不含知识库目录骨架，`INSTALL.md` 没有 Obsidian 部署步骤，适合已有自己知识库、只想接入这套研究纪律的客户；README 里说明了怎么把方法论接到用户自己已有的目录结构上。

两个版本共享同一套核心方法论（`SKILL.md`/`references/`/`skills/`/`templates/`）；未来对方法论本身的修复会视情况同步到两条线，知识库骨架相关的改动只影响 `main`。README 顶部现在会提示用户按自己的情况选版本。

## v2.3.0 — 2026-09-08

**新增**：这个仓库现在同时是一个可以直接用 Obsidian 打开的知识库，而不只是一堆方法论文档。

- 新增知识库目录骨架：`00_首页/`（含 `投研知识库首页.md`、`新手上路.md` 两份导航页）、`00_Inbox/`、`01_每日投研/`、`02_资料收集/结构化数据/`（含 README）、`03_公司研究/`、`04_行业研究/`、`05_投资主题/`、`07_输出与报告/`、`09_附件/`。
- `INSTALL.md` 新增"第 0 步：部署 Obsidian 知识库"——尝试用 `brew`/`winget`/`flatpak`/`snap` 全自动安装 Obsidian 应用本体，失败时才要求用户手动下载安装（唯一允许人工介入的环节）；再用 `obsidian://open?path=...` URI 尝试自动把本仓库打开为 vault，失败时退化为"手动点两下"。
- `AGENTS.md`、`SKILL.md` 同步说明知识库目录的写入规则（原位更新、按类型分文件夹、结构化数据单独沉淀），并明确"这些导航页是给人看的，规则正文仍以 `SKILL.md`/`references/` 为准"，避免 agent 把首页误当规则源。

设计目标：客户克隆仓库、跑一遍 `INSTALL.md`，打开 Obsidian 就能直接开始行业/个股研究，不需要自己另外搭知识库、抄规则。

## v2.2.0 — 2026-09-08

**新增**：`scripts/upgrade-vendored-skills.sh`——检查并升级 `web-access`、`AnySearch` 这两个第三方开源依赖的脚本。会自动 fetch 对比落后情况、本地有未提交改动先 stash、拉取更新、重装依赖，并**实际跑一次搜索/健康检查确认真的能用**（而不是只确认文件已更新）。支持 `--check-only`（只报告不升级）和 `--yes`（跳过交互确认，适合 agent 自动执行）。

**验证记录**（2026-09-08）：本次审计发现 AnySearch 落后上游 2 个大版本（本地约 v2.1.0 → 上游 v3.1.1），中间 v3.0.1 修复过一个"API key 在 HTTP 重定向时可能泄漏"的安全问题；web-access 落后 2 个提交（v2.5.2 → v2.5.4）。已升级并用真实搜索/CDP连接请求验证通过。

**已知遗留**：升级前 `anysearch_cli.py` 上有一份手工补丁（检测"响应看似成功实则是禁用/空结果"的情况），因上游 v3.1.0 已将 CLI 架构从 MCP/JSON-RPC 包装层重写为直连 REST，该补丁修复的旧代码路径已不存在——新架构通过结构化的 `code` 字段判断错误、`data.results` 为空时显式返回"No relevant results found."，等效保护已内建在上游实现里，判定为**不需要再补等价逻辑**。补丁已用 `git stash` 保留在本地 anysearch 仓库的 stash 历史中作为记录。

## v2.1.1 — 2026-09-08

**修复**：`INSTALL.md` 里 AnySearch 的安装方式从"下载 zip 快照"改成 `git clone`——之前的方式装完之后无法用 `git pull` 拿到官方更新，且示例里写死的版本号（v2.1.0）已经过时（上游最新为 v3.1.1，之间有一次"HTTP 重定向可能导致 API key 泄漏"的安全修复）。改成 `git clone` 后和 `web-access` 一样可以直接 `git pull` 升级。

**新增**：`INSTALL.md` 增加"以后怎么升级"一节，说明哪些 skill 跟着本仓库走、哪些需要单独 `git pull`。

## v2.1.0 — 2026-09-08

**新增**：
- `INSTALL.md`：一份写给 agent 执行的一键安装 playbook，覆盖核心依赖安装、可选交易信号环境、三个第三方联网采集工具的自动拉取、批量采集环境、以及最终的健康检查验收，目标是"朋友把仓库链接和一句话丢给自己的 agent，agent 自己装完全部能装的部分"。
- `skills/agent-reach/`：新增第七个 skill，多平台内容抓取（YouTube/B站/GitHub/RSS/V2EX 等）的路由文档；底层 CLI 是独立第三方工具，按 `INSTALL.md` 第 3 步安装。

**变更**：
- README/SKILL.md 的安装说明从"这些是可选外部依赖，自己想办法"改成"按 `INSTALL.md` 一条命令自动拉取"——`web-access`（MIT）和 `AnySearch`（Apache 2.0）都有清晰的自动化安装方式，不需要用户手动摸索。

**说明**：`web-access`、`AnySearch` 的源码本体不随本仓库分发（它们是独立维护的第三方开源项目，直接分发容易变陈旧且不尊重原作者的发布节奏），`INSTALL.md` 改为在安装时从各自官方仓库自动拉取最新版本。

## v2.0.0 — 2026-09-07

全量重写，替换 v1.0.0（2026-08-14，"WorkBuddy 投研工作流包"）。

**新增**：
- 六份方法论说明文档（`references/`）：证据分级与时效、团队协作与角色调度、结论纪律与反方检查、知识库写入规范、数据源能力契约、打分体系回测方法。
- 六个可直接加载运行的真实 skill（`skills/`）：`investment-team-skill`（11 角色委员会完整实现）、`zhiku-research`（知识库写入）、`research-data-verify`（证据语义权威定义）、`quant-check`（打分体系回测）、`stock-data`（结构化行情数据 CLI）、`web-harvest`（联网采集路由）。v1.0.0 只有方法论说明文档，不含可运行的实现。
- 关键变量清单方法论、结论强制收口"镜子测试"、共识差异四问、自由反方六问——这些在 v1.0.0 里没有系统化表述。
- 结构化数据字段规范和关键变量清单两份新模板。
- 一份完整的虚构演示案例（`examples/示例_行业快速判断.md`），端到端演示六步流程。

**变更**：
- G/Y/R 自动化风险门从 v1.0.0 的隐含逻辑变成显式的一等公民概念，贯穿全部文档。
- 知识库写入规范从"建议"升级为带写后自查清单的硬规则。
- 证据分级新增 `claim_type`、`verification_status`、`retrieval_status` 三个正交字段，不再只有粗粒度的 P0-P3。

**剥离**（相比内部完整版本，公开发布时移除的内容）：
- 模型调度/派发基础设施相关内容——这是使用者自己的执行层配置，和投研方法论无关。
- 具体付费数据商（研报/纪要数据库）的私有集成实现，仅保留"这类数据需要什么能力"的描述。
- 与投研方法论无关的私有基础设施：本地 IM/聊天机器人部署脚本、专属数据库客户端。
- 所有开发调试过程中的个人化内容（真实姓名/昵称、账号信息、本地绝对路径），已改写为通用占位符或删除。
- 不属于本次发布范围的历史备份、废弃代码、大体积虚拟环境和调试缓存文件。

**移除的旧文件**（v1.0.0 目录结构，本版本不再沿用）：
- `.codebuddy-plugin/plugin.json`、`skill.yml`：改用 `SKILL.md` 自带的 frontmatter 作为唯一元数据来源，不再维护平行的插件清单文件。
- `验证用例.md` → 重命名为 `验收清单.md`，内容按新方法论重写。

## v1.0.0 — 2026-08-14

初版，"WorkBuddy 投研工作流包"。面向通用 AI 助手，只含方法论说明文档（`SKILL.md` + `references/` + `templates/` + `验证用例.md`），不含可直接运行的 skill 实现。
