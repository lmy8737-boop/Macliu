# Agent 入口

如果你的 agent 运行时按约定读取 `AGENTS.md`（而不是 `SKILL.md`）作为规则入口，这份文件就是你的起点——内容不重复维护，规则正文全部在 [`SKILL.md`](SKILL.md)。

先读 `SKILL.md`，按它的"五步上手路线图"决定接下来读哪些 `references/` 文件；需要实际调用某个 skill 的能力时，进入 `skills/<对应目录>/SKILL.md`。

这份包对任何能读 Markdown、能按文件里的指令行动的 agent 都适用——不限定具体的 agent 框架或运行时。

## 这个仓库同时是一个 Obsidian 知识库

`00_首页/`、`00_Inbox/`、`01_每日投研/`、`02_资料收集/`、`03_公司研究/`、`04_行业研究/`、`05_投资主题/`、`07_输出与报告/`、`09_附件/` 这些目录是研究内容该去的地方，用户很可能直接把这个仓库根目录当 Obsidian vault 打开。写入研究内容时按《知识库写入规范》执行：先搜索这些目录里有没有已经存在的相关笔记，能原位更新就不新建；公司研究进 `03_公司研究/`，行业/产业链进 `04_行业研究/`，跨公司主题进 `05_投资主题/`，可复用的数字沉淀进 `02_资料收集/结构化数据/`。`00_首页/投研知识库首页.md` 和 `00_首页/新手上路.md` 是给人看的导航，不是规则正文，不要因为用户在这个目录下就误以为规则也在这里——规则始终以 `SKILL.md` 和 `references/` 为准。
