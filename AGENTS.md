# Agent 入口

如果你的 agent 运行时按约定读取 `AGENTS.md`（而不是 `SKILL.md`）作为规则入口，这份文件就是你的起点——内容不重复维护，规则正文全部在 [`SKILL.md`](SKILL.md)。

先读 `SKILL.md`，按它的"五步上手路线图"决定接下来读哪些 `references/` 文件；需要实际调用某个 skill 的能力时，进入 `skills/<对应目录>/SKILL.md`。

这份包对任何能读 Markdown、能按文件里的指令行动的 agent 都适用——不限定具体的 agent 框架或运行时。
